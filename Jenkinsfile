pipeline {
    agent any  // Runs on any available agent
    stages {
        stage('Build') {
            steps {
                echo 'Hello, Jenkins! This is a test build.'
            }
        }
    }
}